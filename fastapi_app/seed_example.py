# Placeholder for DB seeding logic
# Expand this file with initial data loading scripts as needed.

from db import SessionLocal
from models import User

# Example
# db = SessionLocal()
# new_user = User(email='admin@example.com')
# db.add(new_user)
# db.commit()
# db.close()
